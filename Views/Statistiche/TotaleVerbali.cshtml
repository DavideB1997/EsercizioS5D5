<h2>Verbali</h2>

@if (Model.Count == 0)
{
    <h3>Non ci sono verbali nel database</h3>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Nome Completo</th>
                <th scope="col">Numero Verbali</th>
                

            </tr>
        </thead>
        <tbody>
            @foreach (var verbaleParziale in Model)
            {
                <tr>
                    <td>@verbaleParziale.NomeCompleto</td>
                    <td>@verbaleParziale.NumeroVerbaliTrascritti</td>
                </tr>
            }
        </tbody>

    </table>
}